import { createGlobalStyle } from 'styled-components';



const GlobalStyles = createGlobalStyle`
:root {
    // site colors to be added in here
}
html{
    // potentially the background can be put in
}
body {
    font-size: 2rem;
}
`